--- a/src/gui/CMakeLists.txt
+++ b/src/gui/CMakeLists.txt
@@ -14,8 +14,8 @@ target_sources(libdosboxcommon PRIVATE
 )
 
 target_link_libraries(libdosboxcommon PRIVATE
-  OpenGL::GL
-  $<IF:$<BOOL:${C_OPENGL}>,libglad,>)
+  $<$<BOOL:${C_OPENGL}>:OpenGL::GL>
+  $<IF:$<BOOL:${C_OPENGL}>,libglad,>)
